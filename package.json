{
  "name": "npm-config-generator",
  "version": "1.0.0",
  "description": "Generates NPM config based on Docker containers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/dockerode": "^3.3.19",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "console-stamp": "^3.1.1",
    "dayjs": "^1.11.9",
    "dockerode": "^4.0.0"
  }
}
